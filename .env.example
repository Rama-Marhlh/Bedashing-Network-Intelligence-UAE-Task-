# Server-side only. Never prefix secrets with NEXT_PUBLIC_.
ANALYST_MODE=llm
AGENT_URL=http://127.0.0.1:8000/ag-ui
LOG_LEVEL=INFO

# Required only when ANALYST_MODE enables an external model.
OPENAI_API_KEY=
AI_MODEL=openai:gpt-5-mini
ANALYST_TIMEOUT_SECONDS=90
